import React from "react";
import "../App.css";

const Navbar: React.FC = () => (
  <nav className="fixed flex top-0 w-full h-10 bg-black">
      <h1 className="text-white">Monggu</h1>
  </nav>
);

export default Navbar;
